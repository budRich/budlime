[
    // { "keys": ["alt+down"], "command": "scroll_width", "args": {"amount": 30 } },
    // { "keys": ["alt+up"], "command": "scroll_width", "args": {"amount": -30 } }

    // horizontal scrolling
    { 
      "keys": ["H"],
      "command": "scroll_width", "args": { "amount": -30},
      "context": [{"key": "setting.command_mode"}] 
    },
    { 
      "keys": ["L"],
      "command": "scroll_width", "args": { "amount": 30},
      "context": [{"key": "setting.command_mode"}] 
    },
]
