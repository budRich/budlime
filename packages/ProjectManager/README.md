`project_manager.py` mod:
(L532): when action is edit:
edit current project instead of showing list.

also added keybinding for edit project:
``` JSON
 { "keys": ["alt+p"], "command": "project_manager", "args": {"action": "edit"}},
```
